public class Main{
    
    public static void main(String[] args){
     String[] str={"aman raj","amit kumar","rani kumari","sana dilnashin"};
//First name from list of full names
     List<String> firstNames=  Arrays.stream(str)
                                     .map(s->s.split(" ")[0])
                                    .collect(Collectors.toList());
        System.out.println(firstNames);
//Joining with delemeter  
          String joinedString= firstNames.stream().collect(Collectors.joining("-"));
          System.out.println(joinedString);
    }  
}
//[aman, amit, rani, sana]
//aman-amit-rani-sana


1.  List<String> str=Stream.concat(list1.stream(),list2.stream()).collect(Collectors.toList());
2.  List<String> items = Arrays.asList("Java", "Python", "C++");
        String result = String.join(", ", items);
        System.out.println(result); // Output: Java, Python, C++
