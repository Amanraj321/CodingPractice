public class Main {
  
    
    public static void main(String[] args) {
       Set<Integer> set= new HashSet<>();
        List<Integer> list=Arrays.asList(1,3,4,6,3,2,5,5,3);
        String str="manraj";
  Set<Integer> list1=list.stream().filter(e->!set.add(e)).collect(Collectors.toSet());
         System.out.println(list1); 
        
       Map<String,List<Integer>> map= list.stream()
            .collect(Collectors.groupingBy(e->e%2==0?"Even":"odd"));
        map.forEach((a,b)->System.out.println(a+""+b));



//PartioningBy

Map<Boolean.List<Integer>> result=list.stream().collect(Collectors.partitioningBy(e->e%2==0));
            
          
    }
}
